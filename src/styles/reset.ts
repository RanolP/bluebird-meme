import { css } from '@emotion/react';

export const Reset = css`
  body {
    margin: 0;
    padding: 0;
  }

  textarea {
    resize: none;
  }

  textarea {
    font-family: inherit;
  }

  textarea {
    outline: none;
  }

  #root {
    max-width: 100vw;
    min-height: 100vh;
  }
`;
